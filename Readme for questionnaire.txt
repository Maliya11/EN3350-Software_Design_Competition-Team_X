1. First, have to run the Back-end(Spring Boot) application using IntelliJ.

Path,
Quesstionaire => Backend

2. Then have to run the Front-end(React) application using VScode.

Path,
Quesstionaire => Frontend_new => quiz

3. Then we can run the Unity project.

Path,
unity_project


Note => The Database is deployed in AWS RDS database and the relevant endpoint is     included in the application.properties file in the Sprint Boot application.